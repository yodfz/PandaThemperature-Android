# OTA 授权密钥配置模板
#
# 用法：复制本文件为同目录下的 ota.properties 并填入真实值。
# ota.properties 已被 .gitignore 排除，不会进入 Git，也不会进入 src.zip。
#
# 缺少该文件时构建**仍然成功**，但 App 的"OTA 授权密钥"输入框为空、
# 不会预填任何值（固件侧缺密钥则会直接编译失败）。
#
# 取值与固件 `src/ble/ota_auth_key.h` 的 `OTA_AUTH_KEY` 必须一致。
#
# 支持两种写法（二者等价，任选其一）：
#   authKey=<16 个可见 ASCII 字符>         例如 16 个可打印字符
#   authKey=<32 位十六进制>                与上者等价的十六进制表示
# 大小写不敏感，允许空格与连字符。
#
# ⚠️ 该密钥只防误触 / 无脑脚本，**不是安全边界**
#    —— 真正拦住恶意固件的是 MCUboot 的 ECDSA-P256 验签；
#    且密钥可从固件镜像里逆向读出。量产应改为每型号/每批次独立的值。

authKey=
