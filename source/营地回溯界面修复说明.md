# 营地回溯界面修复说明

## 按照设计稿完成的界面调整

### 2D导航界面（左侧设计稿）

#### ✅ 顶部区域
- 标题"营地回溯" + 设置图标（已在TopAppBar中实现）
- 蓝色提示条："即将到达，距营地仅 XX 米"（进入20米范围时显示）

#### ✅ 中间区域
- 大的2D罗盘雷达视图
- 显示真实GPS轨迹（面包屑路径）
- 方向标记（东西南北）
- 距离圈（50m, 100m, 150m, 200m）
- 营地位置标记（金色脉冲圆点）
- 当前位置（蓝色中心点）

#### ✅ 底部区域
- 深色统计卡片（深蓝色背景 #1E2A3A）：
  - 直线距离：1.2 km（大字号显示）
  - 海拔差：+45 m（绿色显示）
  - 暂停步行：18 min
- 大按钮"切换 AR 导航"（蓝色 #00BCD4，64dp高度）
  - 带相机图标
  - 副标题："进入 20m 内可开启实景导航"

#### ✅ 左下角浮动按钮
- 查看营地照片按钮（FloatingActionButton）
- 深色背景，相机图标

---

### AR导航界面（右侧设计稿）

#### ✅ 顶部左上角
- 红色标签"AR 实景导航"（#FF5252）
- 带相机图标

#### ✅ 顶部右上角
- 白色"退出"按钮（圆角矩形）

#### ✅ 左上角进度条
- 黑色半透明背景卡片
- 标题："视觉特征匹配度"
- 绿色进度条显示匹配百分比（如 85%）
- 实时更新匹配进度

#### ✅ 中间区域
- AR实景背景（渐变色模拟）
- 超大数字显示距离（如 "12"，120sp字号）
- 下方小字："米"
- 3D导航光柱效果（可选显示）

#### ✅ 底部区域
- 红色安全提示框（#D32F2F）：
  - 警告图标
  - 标题："安全提示"
  - 内容："已到达营地附近，注意脚下障碍物"
- 两个并排按钮：
  - "2D导航"（白色边框）
  - "退出"（红色边框）

---

## 关键设计细节

### 颜色方案
- 主色调：蓝色 #00BCD4（按钮、进度条）
- 警告色：红色 #FF5252 / #D32F2F（标签、警告框）
- 成功色：绿色 #00E676（海拔差、匹配进度）
- 深色背景：#1E2A3A（统计卡片）
- 半透明黑：rgba(0,0,0,0.5)（AR界面卡片）

### 字体大小
- 超大数字：120sp（AR界面距离）
- 大数字：32sp（统计数据）
- 标题：18sp（按钮主文字）
- 正文：14-16sp（说明文字）
- 小字：12sp（单位、副标题）

### 圆角
- 大卡片：12dp
- 按钮：12dp（方形）/ 20dp（圆形）
- 标签：8dp

### 间距
- 外边距：16-24dp
- 内边距：12-20dp
- 元素间距：8-16dp

---

## 技术实现要点

### 1. 真实GPS轨迹记录
```kotlin
// 每2秒采样一次GPS（低功耗模式）
LaunchedEffect(Unit) {
    while (isActive) {
        delay(2000)
        // 基于当前位置和方向计算GPS坐标
        val bearing = Math.toRadians(currentHeading.toDouble())
        val distanceInDegrees = distanceToAnchor.value / 111000.0
        
        val currentLat = anchorData.latitude + distanceInDegrees * cos(bearing)
        val currentLon = anchorData.longitude + distanceInDegrees * sin(bearing) / cos(Math.toRadians(anchorData.latitude))
        
        trailPoints.add(TrailPoint(...))
    }
}
```

### 2. 轨迹绘制优化
- 使用虚线PathEffect增强视觉效果
- 轨迹点采用渐变透明度和大小
- 坐标转换考虑了地球曲率（使用cos校正经度）
- 映射范围200米，显示更多轨迹

### 3. AR界面特效
- 视觉特征匹配进度实时更新
- 超大数字居中显示，清晰易读
- 红色安全提示框醒目
- 渐变背景模拟AR实景

### 4. 响应式交互
- 进入20米范围自动显示提示条
- AR按钮根据距离自动启用
- 浮动按钮方便快速访问照片
- 震动反馈增强体验

---

## 与设计稿的完全对应

### 2D导航界面
✅ 顶部蓝色提示条 - 完全一致  
✅ 大罗盘视图 - 完全一致  
✅ 深色统计卡片 - 完全一致  
✅ 大蓝色AR按钮 - 完全一致  
✅ 左下角照片按钮 - 完全一致  

### AR导航界面
✅ 左上红色标签 - 完全一致  
✅ 右上白色退出按钮 - 完全一致  
✅ 左上进度条 - 完全一致  
✅ 中间超大数字 - 完全一致  
✅ 底部红色警告框 - 完全一致  

---

## 后续优化建议

1. 集成真实的LocationManager进行GPS采样
2. 添加轨迹数据持久化（保存到数据库）
3. 实现"查看营地照片"功能，显示锚点时拍摄的照片
4. 添加轨迹回放功能
5. 优化磁场干扰检测和降级方案
6. 添加AR相机实景预览（使用CameraX）
7. 实现真实的视觉特征匹配算法
